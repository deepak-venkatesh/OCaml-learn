let () = print_string "Hello World!\n"

(*

In terminal we can compile with
> ocamlc hello_world.ml
or
> ocamlc -o hello hello_world.ml

In the former case the output is a.out and in the latter it is hello

>./a.out
or
> ./hello

Output will be printed


*)